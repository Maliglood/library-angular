Проблемы Angular'а:
1) По собственной невнимательности не мог сначала завести проект (не хватало main.ts). После подсказки, все заработало довольно быстро.
2) Студия часто показывает ошибки, которых для Webpack'а нет. Так же по дефолту в проекте стоит билд фронтенда. Из-за этого Билд валился.
	Надо отключать встроенный билд (02-var-02.png) и привязывать к билду вебпак.
3) Проблемы с использованием templateUrl:
	a) Нужно указать загрузчик html в конфигах Webpack'а;
	b) Описание/привязка методов к событиям в шаблоне не проверяется компилятором. Например, я скопировал пример, переименовал в файле .ts
		название метода, а в шаблоне нет (скрин a-03-b.png). Из-за этого вылазила ошибка, хотя собиралось все без проблем.
		
Проблемы React'а:
1) Библиотека написана не на TypeScript'е. Как следствие, если хотим TypeScript в проекте, то нужно подтягивать вские тайпинги и заниматься с ними любовью (файл r-01-01.png).
2) --------//--------