Общие проблемы:
1) Студия часто показывает ошибки, которых для Webpack'а нет. Так же по дефолту в проекте стоит билд фронтенда. Из-за этого Билд валился.
	Надо отключать встроенный билд (скрин 01-var_02.png) и привязывать к билду вебпак (пока не получилось, с Gulp'ом получалось).
2)

Сравнение:
1) Порог вхождения.
2) Собирание проекта в один файл.
3) Вынос верстки в отдельные файлы.
4) Наличие готовых компонент.
5) Маршрутизация.

Проблемы Angular'а:
1) По собственной невнимательности не мог сначала завести проект (не хватало main.ts). После подсказки, все заработало довольно быстро.
2) Webpack собрал мои файлы вместе с Ангуляром в один файл, который я просто подключил к странице.
3) Проблемы с использованием templateUrl:
	a) Нужно указать загрузчик html в конфигах Webpack'а;
	b) Описание/привязка методов к событиям в шаблоне не проверяется компилятором. Например, я скопировал пример, переименовал в файле .ts
		название метода, а в шаблоне нет (скрин angular-03-b.png). Из-за этого вылазила ошибка, хотя собиралось все без проблем.
		
Проблемы React'а:
1) Библиотека написана не на TypeScript'е. Как следствие, если хотим TypeScript в проекте, то нужно подтягивать definition-файлы и заниматься
	с ними любовью:
	a) Изначально я пытался подтягивать definition-файлы через @types (скрин react-01-a.png). Webpack выдавал нелогичную ошибку, хотя у Вадима
		из SE все собиралось (но не в студии). Затем я заменил @types на готовый definition-файл из другого проекта - и все стало собираться.
	b) Нужно не забывать в начале каждого .tsx файла указывать xml-комментарий с ссылкой на definition-файл,
		причем из-за вложенности ссылку надо менять (или все же нет?).
	с) Проблему с подчеркиванием элементов Реакта в студии я так и не смог решить (скрин react-01-с.png).
		Дополнительная проблема: не смог вкладывать .js в .tsx в дереве проекта в студии + выглядит так, будто студия сама генирит .js из .tsx
		после любого изменения.
2) Оказалось, что Webpack не собирает в итоговый файл Реакт. Мб это как-то и можно сделать, но об этом мы узнаем уже в следующем докладе.
	По началу я не мог определить ошибку, потом понял, в чем дело, и дополнительно подключил к странице Реакт.
3)